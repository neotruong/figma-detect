# Data Folders

This project stores runtime comparison artifacts under `data/`.

- `baselines/`: one latest image per case (`<caseName>.png`)
- `report-history/`: time-based JSON and diff image history

These folders are used by `POST /api/figma/sync/:caseName`.
